services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: restaurante_financeiro
    ports:
      - "5432:5432"
    volumes:
      - restaurante_pgdata:/var/lib/postgresql/data
volumes:
  restaurante_pgdata:
