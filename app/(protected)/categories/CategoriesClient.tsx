"use client";import{FormEvent,useEffect,useState}from"react";type C={id:string;name:string;type:string;active:boolean};const categoryLabels:Record<string,string>={EXPENSE:"Despesa",INCOME:"Receita",BOTH:"Receita e despesa"};export default function CategoriesClient(){const[rows,setRows]=useState<C[]>([]);async function load(){const r=await fetch("/api/categories");if(r.ok)setRows(await r.json())}useEffect(()=>{load()},[]);async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);const r=await fetch("/api/categories",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:f.get("name"),type:f.get("type")})});if(!r.ok){alert((await r.json()).error);return}form.reset();load()}return <main className="content"><h1 className="page-title">Categorias</h1><p className="page-subtitle">Classifique receitas e despesas para relatórios úteis.</p><div className="card"><form className="form-grid" onSubmit={add}><div className="field span-2"><label>Nome</label><input name="name" required/></div><div className="field"><label>Tipo</label><select name="type"><option value="EXPENSE">Despesa</option><option value="INCOME">Receita</option><option value="BOTH">Receita e despesa</option></select></div><div style={{alignSelf:"end"}}><button className="btn primary">Criar categoria</button></div></form></div><div className="card" style={{marginTop:16}}><div className="table-wrap"><table><thead><tr><th>Categoria</th><th>Tipo</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{categoryLabels[x.type]||x.type}</td><td>{x.active?"Ativa":"Inativa"}</td></tr>)}</tbody></table></div></div></main>}
