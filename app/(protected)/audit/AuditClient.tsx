"use client";import{useEffect,useState}from"react";type A={id:string;action:string;entity:string;entityId?:string;createdAt:string;actor:{name:string;email:string};data?:unknown};const actionLabels:Record<string,string>={CREATE:"Criação",UPDATE:"Alteração",DELETE:"Exclusão",DEACTIVATE:"Desativação",REACTIVATE:"Reativação",ADD_MEMBER:"Usuário adicionado",PAY:"Pagamento",STOCK_MOVEMENT:"Movimentação de estoque",OPEN:"Abertura",CLOSE:"Fechamento"};const entityLabels:Record<string,string>={Transaction:"Movimentação financeira",Account:"Conta / caixa",Category:"Categoria",Supplier:"Fornecedor",AccountsPayable:"Conta a pagar",Employee:"Funcionário",PayrollEntry:"Folha de pagamento",InventoryItem:"Item de estoque",RestaurantMember:"Usuário do sistema",CashSession:"Sessão de caixa"};export default function C(){const[rows,setRows]=useState<A[]>([]);const[error,setError]=useState("");useEffect(()=>{fetch("/api/audit").then(async r=>{if(!r.ok){setError((await r.json()).error);return}setRows(await r.json())})},[]);return <main className="content"><h1 className="page-title">Auditoria</h1><p className="page-subtitle">Registro das principais alterações feitas no sistema.</p>{error&&<div className="notice error">{error}</div>}<div className="card"><div className="table-wrap"><table><thead><tr><th>Data</th><th>Usuário</th><th>Ação</th><th>Entidade</th><th>ID</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{new Date(x.createdAt).toLocaleString("pt-BR")}</td><td>{x.actor.name}<br/><small className="muted">{x.actor.email}</small></td><td>{actionLabels[x.action]||x.action}</td><td>{entityLabels[x.entity]||x.entity}</td><td><code>{x.entityId||"—"}</code></td></tr>)}</tbody></table></div></div></main>}
