"use client";import{useState}from"react";export default function C(){const[from,setFrom]=useState("");const[to,setTo]=useState("");const href=`/api/export?${from?`from=${from}&`:""}${to?`to=${to}`:""}`;return <main className="content"><h1 className="page-title">Relatórios</h1><p className="page-subtitle">Exporte as movimentações para Excel e encaminhe ao contador.</p><div className="card"><div className="form-grid"><div className="field"><label>De</label><input type="date" value={from} onChange={e=>setFrom(e.target.value)}/></div><div className="field"><label>Até</label><input type="date" value={to} onChange={e=>setTo(e.target.value)}/></div><div className="span-2 actions" style={{alignSelf:"end"}}><a className="btn primary" href={href}>Baixar Excel</a></div></div></div><div className="notice" style={{marginTop:16}}>O arquivo exportado contém data, tipo, descrição, categoria, conta, forma de pagamento, valor, usuário e observação. Para escrituração contábil oficial, valide o formato necessário com o contador.</div></main>}
