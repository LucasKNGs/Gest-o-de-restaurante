"use client";import{FormEvent,useEffect,useState}from"react";type M={id:string;role:string;active:boolean;user:{name:string;email:string}};const roleLabels:Record<string,string>={OWNER:"Proprietário",ADMIN:"Administrador",MANAGER:"Gerente",OPERATOR:"Operador",VIEWER:"Somente leitura"};export default function C(){const[rows,setRows]=useState<M[]>([]);async function load(){const r=await fetch("/api/team");if(r.ok)setRows(await r.json())}useEffect(()=>{load()},[]);async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);const r=await fetch("/api/team",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:f.get("name"),email:f.get("email"),password:f.get("password"),role:f.get("role")})});if(!r.ok){alert((await r.json()).error);return}form.reset();load()}return <main className="content"><h1 className="page-title">Equipe de acesso</h1><p className="page-subtitle">Usuários do sistema. Funcionários do restaurante são um cadastro separado.</p><div className="card"><form className="form-grid" onSubmit={add}><div className="field"><label>Nome</label><input name="name" required/></div><div className="field"><label>E-mail</label><input name="email" type="email" required/></div><div className="field"><label>Senha inicial</label><input name="password" type="password" minLength={8} required/></div><div className="field"><label>Papel</label><select name="role"><option value="OPERATOR">Operador</option><option value="VIEWER">Somente leitura</option><option value="MANAGER">Gerente</option><option value="ADMIN">Administrador</option></select></div><div className="span-4"><button className="btn primary">Adicionar usuário</button></div></form></div><div className="card" style={{marginTop:16}}><div className="table-wrap"><table><thead><tr><th>Nome</th><th>E-mail</th><th>Papel</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.user.name}</td><td>{x.user.email}</td><td><span className="badge">{roleLabels[x.role]||x.role}</span></td><td>{x.active?"Ativo":"Inativo"}</td></tr>)}</tbody></table></div></div><div className="notice" style={{marginTop:16}}>Proprietário e Administrador podem cadastrar usuários. Gerente administra a operação. Operador registra dados. Somente leitura pode apenas consultar.</div></main>}
