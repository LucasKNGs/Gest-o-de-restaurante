"use client";import{FormEvent,useEffect,useState}from"react";type S={id:string;name:string;document?:string;phone?:string;email?:string;active:boolean};export default function C(){const[rows,setRows]=useState<S[]>([]);async function load(){const r=await fetch("/api/suppliers");if(r.ok)setRows(await r.json())}useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);const body={name:f.get("name"),document:f.get("document")||null,phone:f.get("phone")||null,email:f.get("email")||null};const r=await fetch("/api/suppliers",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});if(!r.ok){alert((await r.json()).error);return}form.reset();load()}async function del(id:string){const r=await fetch(`/api/suppliers/${id}`,{method:"DELETE"});if(!r.ok){alert((await r.json()).error);return}load()}return <main className="content"><h1 className="page-title">Fornecedores</h1><p className="page-subtitle">Cadastro para vincular despesas e contas futuras.</p><div className="card"><form className="form-grid" onSubmit={submit}><div className="field"><label>Nome</label><input name="name" required/></div><div className="field"><label>CNPJ/CPF</label><input name="document"/></div><div className="field"><label>Telefone</label><input name="phone"/></div><div className="field"><label>E-mail</label><input name="email" type="email"/></div><div className="span-4"><button className="btn primary">Cadastrar fornecedor</button></div></form></div><div className="card" style={{marginTop:16}}><div className="table-wrap"><table><thead><tr><th>Fornecedor</th><th>Documento</th><th>Telefone</th><th>E-mail</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.document||"—"}</td><td>{x.phone||"—"}</td><td>{x.email||"—"}</td><td>{x.active?"Ativo":"Inativo"}</td><td>{x.active&&<button className="btn small danger" onClick={()=>del(x.id)}>Desativar</button>}</td></tr>)}</tbody></table></div></div></main>}
